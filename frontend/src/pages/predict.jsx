import {Flex} from "@chakra-ui/react";

const Predict = () => {
    return (
        <Flex p={12} w={'full'} h={'full'} flexDirection={'column'}>
            PREDICT
        </Flex>
    )
}

export default Predict